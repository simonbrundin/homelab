apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: immich
  namespace: immich
spec:
  interval: 5m
  chartRef:
    kind: OCIRepository
    name: immich
    namespace: flux-system
  values:
    # Server component (formerly controllers.main)
    server:
      enabled: true
      controllers:
        main:
          containers:
            main:
              image:
                tag: v1.133.0
              env:
                DB_HOSTNAME:
                  value: immich-db-rw
                DB_USERNAME:
                  value: immich
                DB_PASSWORD:
                  valueFrom:
                    secretKeyRef:
                      name: immich-db-app
                      key: password
                DB_DATABASE_NAME:
                  value: immich
                DB_VECTOR_EXTENSION:
                  value: pgvector
                REDIS_HOSTNAME:
                  value: immich-valkey
                IMMICH_MACHINE_LEARNING_URL:
                  value: http://immich-machine-learning:3003
    # Machine learning component
    machine-learning:
      enabled: true
      controllers:
        main:
          containers:
            main:
              image:
                tag: v1.133.0
    # Data persistence (library photos)
    immich:
      persistence:
        library:
          existingClaim: immich-library
    # Redis/valkey for job queues
    valkey:
      enabled: true
      persistence:
        data:
          enabled: true
          size: 1Gi
          accessMode: ReadWriteOnce
          type: persistentVolumeClaim
  install:
    createNamespace: true
    timeout: 10m
  upgrade:
    timeout: 10m
