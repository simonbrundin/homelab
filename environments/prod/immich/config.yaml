apiVersion: v1
kind: ConfigMap
metadata:
  name: immich-config
  namespace: immich
  labels:
    app.kubernetes.io/name: immich
data:
  immich.yaml: |
    database:
      host: immich-db-rw
      username: immich
      database: immich
    redis:
      hostname: immich-valkey
    machine_learning:
      url: http://immich-machine-learning:3003
