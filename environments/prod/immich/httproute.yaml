apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: immich-route
  namespace: immich
spec:
  parentRefs:
    - name: simons-gateway
      namespace: envoy
  hostnames:
    - "photos.simonbrundin.com"
  rules:
    - backendRefs:
        - name: immich-server
          port: 2283
